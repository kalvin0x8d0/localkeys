services:
  localkeys:
    build: .
    container_name: localkeys-web
    ports:
      - "8080:8080"
    restart: unless-stopped
    # We no longer need volume mounts or interactive TTY 
    # since everything is handled via the web browser.