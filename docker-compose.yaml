services:
  localkeys:
    build: .
    container_name: localkeys-web
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - PORT=${PORT:-8080}
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
